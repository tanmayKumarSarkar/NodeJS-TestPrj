<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Profile Page</title>
    <style>
      body{background: lightblue; font-family: verdena; color: #fff; padding: 30px;}
      h1{font-size: 48px; text-transform: uppercase; letter-spacing: 2px; text-align: center;}
      p,ul{font-size: 16px; text-align: center}
    </style>
  </head>
  <body>
    <% include partials/nav.ejs %>
    <h1>Welcome to profile of <%= person %> </h1>
    <p><strong>Age : <%= data.age %></strong></p>
    <p><strong>Job : <%= data.job %></strong></p>
    <p><strong>Hobbies</strong>
    <ul><% data.hobbies.forEach((item)=> { %>
      <li><%=item%></li>
      <% }); %>
    </ul></p>
  </body>
</html>
